import { z } from "zod";
export default z.object({ "productReferenceId": z.string().min(1).describe("Product Id."), "recurringDataBlocks": z.array(z.object({ "productId": z.string().min(1).describe("Product Id for data blocks"), "count": z.number().int().describe("Quantity of the specified data block to add") }).strict()).nullable().describe("Recurring data blocks to configure.").optional(), "existingDataPoolId": z.string().nullable().describe("Not yet supported. Reserved for future enhancements.").optional(), "delayUpdate": z.boolean().nullable().describe("Specifies when the change will be applied. True forces changes to be scheduled on the next billing cycle, False forces changes to occur immediately, Null uses legacy logic allowing upgrades to occur immediately and downgrades to be delayed.").optional() }).strict();
//# sourceMappingURL=zod-UpdateServiceLineProductRequest.js.map